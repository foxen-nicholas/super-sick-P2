<% if (alerts.error) { %>
  <% alerts.error.forEach(function(message) { %>
    <div style="background-color: red; color: white;">
      <%= message %>
    </div>
    <% }); %>
<% } else if (alerts.succes) { %>
  <% alerts.success.forEach(function(message) { %>
    <div style="backgroun: green; color: white">
      <%= message %>
    </div>
  <% }); %>
<% } %>
